language: elixir
elixir:
- 1.4.1
otp_release:
- 18.2
sudo: false
addons:
  postgresql: '9.6.1'
services:
  - postgresql
before_script:
# Create the test database
- psql -c 'create database social_app_api_test;' -U postgres
